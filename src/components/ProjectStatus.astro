---
interface Props {
  votosCount: number;
  votacionCompleta: boolean;
  hasUserVoted: boolean;
  votacionAbierta?: boolean;
}

const { votosCount, votacionCompleta, hasUserVoted, votacionAbierta = true } = Astro.props;
---

<div class="inline-flex items-center gap-2">
  {!votacionAbierta ? (
    <span class="badge-cerrada">
      🔒 Votación cerrada
    </span>
  ) : votacionCompleta ? (
    <span class="badge-completa">
      ✅ Votación completa ({votosCount}/3)
    </span>
  ) : hasUserVoted ? (
    <span class="badge-esperando">
      ⏳ Esperando votos ({votosCount}/3)
    </span>
  ) : (
    <span class="badge-pendiente">
      📬 Pendiente de tu voto
    </span>
  )}
</div>
